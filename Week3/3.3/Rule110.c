#include <stdio.h>
#include <assert.h>
#define ARRAYWIDTH 50

enum input {central, random}
typedef enum input input

void test(void);

int main(void) {
  int currentArray[ARRAYWIDTH], newArray[ARRAYWIDTH], userInput = 1;

  return 0;
}

void nextNumber(int *currentArray, int *newArray, int i) {

}

void initialiseArray(int *currentArray, int userInput) {
  if (userInput == ) {

  }
}
